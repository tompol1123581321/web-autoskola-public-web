---
import headImg from "../assets/hero.jpeg";
import Link from "../components/ui/link.astro";
import { Icon } from "astro-icon";
import { Image } from "astro:assets";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <!-- Obrázek v hero sekci -->
  <div class="py-6 md:order-1 hidden md:block">
    <Image
      src={headImg}
      alt="Motocyklový výcvik"
      sizes="(max-width: 800px) 100vw, 620px"
      class="rounded-md"
      loading="eager"
    />
  </div>

  <!-- Textová část hero sekce -->
  <div>
    <h1
      class="text-4xl lg:text-5xl xl:text-6xl font-bold lg:tracking-tight xl:tracking-tighter">
      Autoškola Luboš Hlaváček
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      Provádíme výuku a výcvik řidičů pro skupiny A2, A a B – osobní automobil, 
      motocykly a jejich rozšíření. Využijte možnost moderních učeben, 
      individuální přístup a spoustu praktických rad od 
      zkušených instruktorů.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <!-- Odkaz na přihlášky -->
      <Link
        href="/prihlaseni"
        class="flex gap-1 items-center justify-center bg-blue-700"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="mdi:register-outline" />
        Přihlášky
      </Link>

      <!-- Odkaz na galerii -->
      <Link
        href="#gallery"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="fa:photo" />
        Galerie
      </Link>

      <!-- Odkaz na tým -->
      <Link
        href="#team"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="ri:team-line" />
        Tým
      </Link>
    </div>

    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <!-- Odkaz na novinky -->
      <Link
        href="#news"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="fluent:news-20-regular" />
        Novinky (poslední změna: 7.1.2025)
      </Link>
    </div>
  </div>
</main>
