<div class="mt-6 sm:mt-16 text-left sm:text-center">
  <h1 class="text-4xl lg:text-5xl font-bold lg:tracking-tight">
    <slot name="title">Title</slot>
  </h1>
  {Astro.slots.has("desc") && (
    <p class="text-lg mt-4 text-slate-600">
      <slot name="desc" />
    </p>
  )}
</div>
